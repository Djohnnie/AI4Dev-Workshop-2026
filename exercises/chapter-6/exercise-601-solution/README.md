# Exercise 601 Solution — Short URL Discovery Sprint

This folder contains a **possible finished result** for Exercise 601.

Contents:

- `ShortUrlWorkshop/` — runnable .NET 10 minimal API
- `ShortUrlWorkshop.Tests/` — unit tests for the service behavior
- `analysis/` — example analysis artifacts produced before implementation

Run the app:

```bash
dotnet run --project ShortUrlWorkshop/ShortUrlWorkshop.csproj
```

Run the tests:

```bash
dotnet test ShortUrlWorkshop.Tests/ShortUrlWorkshop.Tests.csproj
```
